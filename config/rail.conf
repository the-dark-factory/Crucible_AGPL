# CRUCIBLE — the inference rail.
#
# RULING (Tony, 2026-09-11 16:52): "leave Rosie in place at the moment, but include a
# method to repoint the llm." This file IS that method. Edit it and restart; no rebuild.
#
# ⛔ NOT an environment variable, and not an argument. mui:edition-is-compiled-not-argued
# forbids Ada.Environment_Variables and Ada.Command_Line anywhere in src/, so that the
# EDITION cannot be talked out of its licence refusal. The rail is a different kind of
# thing — model choice moves cost, sovereignty and yield, never CORRECTNESS, because the
# proof gate is what decides correctness (project_model_agnostic_factory). So the rail is
# configuration; the edition is not.
#
# ⚠ A repoint is a REAL CHANGE OF PROVENANCE. Whatever is set here is recorded in the
# provenance of everything forged under it (obligation 9). Changing it silently is how a
# stranger fails to re-derive a result.

model    = "qwen3.8-27b-ada:v0.3"
endpoint = "http://127.0.0.1:11435"
rail     = "ollama"

# Rosie v0.3 — the sovereign local head, S0, no egress. Left in place 2026-09-11 by ruling.
# Measured, not assumed: proofbench 2026-09-08 single-shot, v0.3 PROVED=33 vs v0.4 PROVED=14
# over 58 tasks (ceiling ~53; at least 5 unpassable by construction). v0.4 is NOT an upgrade.
#
# Known alternatives, for whoever repoints this:
#   qwen3.8:27b            the base v0.3 was fine-tuned from
#   claude-cli haiku/opus  the frontier rail; the body-fill lane escalates haiku -> opus
#                          after one refusal. NOT sovereign — S2 egress.
