name = "crucible"
description = "CRUCIBLE — the release factory. Monolithic Ada SPARK; judges its own work."
version = "0.2.1"
licenses = "AGPL-3.0-or-later"
maintainers = ["Anthony Gair <tony.gair@thedarkfactory.co.uk>"]
maintainers-logins = ["tonygair"]
project-files = ["crucible.gpr"]

# ⛔ NO [[depends-on]] SECTION, DELIBERATELY.
# The forge calls zero crates — measured, not assumed (RELEASE_ghillie_pipeline_crate_list
# 2026-09). A round is a sealed room: one directory, one specification, no dependency
# graph, because the proof obligation is per-file. mui:crucible-depends-on-no-crate holds
# this as a standing claim rather than an accident.
